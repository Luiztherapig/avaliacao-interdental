export const DEFAULT_UNIT_SLUG = "interdental";

export const SURVEY_SOURCES = ["qr", "whatsapp", "direct"] as const;
export const QUESTION_TYPES = [
  "emoji_rating",
  "rating",
  "yes_no",
  "single_choice",
  "text"
] as const;
export const CLASSIFICATIONS = ["elogio", "neutro", "atencao", "critico"] as const;
